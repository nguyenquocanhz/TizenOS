// Quản lý đầu ra hiển thị (Màn hình)
// Lắng nghe sự kiện new_output từ wlr_backend để cấu hình màn hình mới
// Xử lý sự kiện frame để render các cửa sổ bằng wlr_renderer
// Hỗ trợ hiển thị GTK4 layer shell
